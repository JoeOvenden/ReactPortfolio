import { registerUser } from "@/app/lib/users";
import { test, expect } from "vitest";
import { RegisterUserInputs, registerUserSchema } from "@/app/lib/definitions/User";

test('Register user', () => {

});